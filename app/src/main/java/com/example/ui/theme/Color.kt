package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Polish - Material 3 Light Theme Colors (High Contrast)
val BackgroundLight = Color(0xFFFDF8FD)      // Soft, warm light canvas
val SurfaceLight = Color(0xFFF3EDF7)         // Material You neutral surface card
val CardInnerWhite = Color(0xFFFFFFFF)       // Crisp white background for forms and nested items
val OutlineBorder = Color(0xFFCAC4D0)        // Material 3 border/divider line

val PrimaryPurple = Color(0xFF6750A4)        // Rich M3 purple for primary buttons/accents
val PrimaryContainer = Color(0xFFEADDFF)     // Elegant lavender container highlighting
val OnPrimaryContainer = Color(0xFF21005D)   // High contrast deep purple text

val TextPrimary = Color(0xFF1C1B1F)          // Near-black charcoal for supreme scanning readability
val TextSecondary = Color(0xFF49454F)        // Compact medium charcoal for secondary detail
val PureWhite = Color(0xFFFFFFFF)            // Absolute white (specifically for text on dark buttons)

val SuccessGreen = Color(0xFF128C7E)         // Deep moss/teal green for success badges & trigger buttons
val DangerRed = Color(0xFFB3261E)            // Assertive crimson red for alerts
val AmberContrast = Color(0xFF8F4E00)        // Dense amber-orange with deep contrast ratio

// High-contrast pink/blue styling from Professional Polish palette
val PinkBadgeBg = Color(0xFFFFD8E4)          // Soft warm pink badge decoration
val PinkBadgeText = Color(0xFF31111D)        // Intense maroon text
val BlueBadgeBg = Color(0xFFC2E7FF)          // Airy ice-blue badge decoration
val BlueBadgeText = Color(0xFF001D35)        // Midnight blue text
